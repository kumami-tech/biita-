.Post_index_wrapper
  .Post_index_wrapper__Inner
    .Post_index_left
      .Title ツアーゲストを探す
      .Search_title.Bold
        条件で絞り込む
      .Keyword
        = form_with(url: posts_g_index_path, local: true, method: :get, class: "Keyword__Form") do |f|
          .Keyword__Form__Input
            = f.text_field :keyword, placeholder: "キーワードを入力", class: "Keyword__Form__Input__Text"
            = button_tag type: 'submit', class: "Keyword__Form__Input__Btn" do
              = icon('fas', 'search')
      .Tag_list
        - @tags.each do |tag|
          = link_to posts_g_index_path(tag_name: tag) do
            .tag
              = tag
    .Post_index_right
      .Title ツアーゲストの投稿一覧
      .Post_index_title
        = icon('fas', 'list')
        = "投稿一覧"
      - if @posts.empty?
        = render "layouts/empty"
      - else
        .Post_index
          - @posts.each do |post|
            = render partial: "post", locals: { post: post }