.Wrapper
  .Title
    = "#{@user.name}さんのページ"
  .Profile
    = image_tag @user.profile_image.url, fallback: "no-image.jpg", class: "Profile__Image"
    .Profile__Content
      .Profile__Content__Name_btns
        .Name
          = @user.name
        - if @user.id == current_user.id
          .White_btns
            = link_to "編集", edit_user_path(current_user), class: "White_btns__Btn"
            = link_to "トークルーム", groups_path, class: "White_btns__Btn"
            = link_to "レビュー", "#", class: "White_btns__Btn"
        - else
          .White_btns
            - if @group_id.nil?
              = link_to "トークルーム", "/groups/#{@user.id}/create", class: "White_btns__Btn"
            - else
              = link_to "トークルーム", group_messages_path(@group_id, user_id: @user.id), class: "White_btns__Btn"
            = link_to "レビュー", "/users/#{@user.id}/reviews", class: "White_btns__Btn"
      .Profile__Content__Text.Text
        = simple_format(@user.profile)

  .Position
    = link_to "ツアーゲスト", posts_g_path(@user.id), class: "Link"
    = link_to "ツアーキャスト", posts_c_path(@user.id), class: "Bold Link"

  - if @posts.empty?
    .Empty 投稿されていません

  - @posts.each do |post|
    = render partial: "post", locals: { post: post }